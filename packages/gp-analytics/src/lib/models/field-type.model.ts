/**
 * Field types representing supported data formats in the analytical schema.
 */
export enum FieldType {
  String = 'string',
  Integer = 'integer',
  Decimal = 'decimal',
  Number = 'number',
  Currency = 'currency',
  Boolean = 'boolean',
  Date = 'date',
  Time = 'time',
  DateTime = 'datetime',
  Guid = 'guid',
  Json = 'json',
}
