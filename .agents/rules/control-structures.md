# Control Structure Bracing Guidelines

- Always use curly braces `{ ... }` for all `if`, `else`, `for`, `while`, and `do` control structures.
- Single-line brace-less statements (e.g. `if (condition) return;`) are strictly prohibited.
